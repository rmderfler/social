<!DOCTYPE html>
<html>
<head>
  <title>Social</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  <nav class="navbar navbar-inverse fixed navbar-custom" role="navigation">
    <div class="container">
      <ul class="nav navbar-nav title">
        <% if user_signed_in? %>
          Logged in as <strong><%= current_user.email %></strong>.
          <p><%= link_to 'Edit profile', edit_user_registration_path %> |
          <%= link_to "Sign out", destroy_user_session_path, method: :delete %> |

          <%= link_to "Your home page", user_path(current_user) %> |
          <%= link_to "Send letter", user_path(current_user) %> |
          <%= link_to "List Users", users_path %></p>

        <% else %>
          <%= link_to "Sign up", new_user_path %> |
          <%= link_to "Sign in", new_user_session_path %>
        <% end %>
      </ul>
    </div>
  </nav>
<body>
  <div class="container">
    <div class="alert">
      <%= flash[:alert] %>
      <%= flash[:notice] %>

    </div>
    <%= yield %><br/>
    <%= debug(params) %>
  </div>
</body>
</html>
