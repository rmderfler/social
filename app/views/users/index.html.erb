<h1>Listing users</h1>

<table>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <tr><td><%=h user.email %></td><td><%= link_to "Become Friend", friendships_path(:friend_id => user), :method => :post %></td></tr>
      
      </tr>
    <% end %>
  </tbody>
</table>

<br>
